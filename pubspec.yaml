# name: kumung_gagge
# description: 거래 위치 확인이 가능한 상품 상세 페이지

# environment:
#   sdk: ">=3.0.0 <4.0.0"   # Flutter 3.x 호환

# dependencies:
#   flutter:
#     sdk: flutter
#   flutter_naver_map: ^1.3.0
#   intl: ^0.19.0
#   timeago: ^3.6.0
#   geolocator: ^9.0.2
#   url_launcher: ^6.3.0
#   image_picker: ^1.1.2
#   go_router: ^16.1.0

# flutter:
#   uses-material-design: true

# dev_dependencies:
#   flutter_test:
#     sdk: flutter
#   flutter_lints: ^3.0.0

name: kumeong_store
description: KU-멍가게 Flutter 앱

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_naver_map: ^1.3.0
  intl: ^0.19.0
  timeago: ^3.6.0
  geolocator: ^9.0.2
  url_launcher: ^6.3.0
  image_picker: ^1.1.2
  go_router: ^16.1.0
  flutter_rating_bar: ^4.0.1

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
