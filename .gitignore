# =========================
# OS & Editors / Tools
# =========================
.DS_Store
*.log
*.swp
*.pyc
*.class
.history
.svn/
.atom/
.idea/
*.iml
*.ipr
*.iws

# VS Code: 워크스페이스 설정은 팀 공유용으로 허용(민감값 넣지 말 것)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# =========================
# Flutter / Dart
# =========================
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.pub/
.pub-cache/
.fvm/
**/build/
**/doc/api/
**/ios/Flutter/.last_build_id
.metadata
coverage/

# 심볼/난독화 산출물
app.*.symbols
app.*.map.json

# pubspec.lock: 앱은 보통 커밋 유지(패키지라면 무시 고려)
# (무시하지 않음)

# =========================
# Android (Gradle, NDK, Local keys)
# =========================
/android/local.properties
/android/app/debug
/android/app/profile
/android/app/release
.android/
.gradle/
.cxx/

# Android signing / keys (절대 커밋 금지)
android/key.properties
*.keystore
*.jks
*.keystore.properties

# Firebase(Google) service keys (절대 커밋 금지)
android/app/google-services.json
# flavor 분리 시 경로 추가 예: android/app/src/**/google-services.json

# 과거 백업/임시
android_old/

# =========================
# iOS
# =========================
ios/Pods/
# ios/Podfile.lock  # 보통 커밋 유지 권장

# iOS 빌드/서명 산출물
ios/DerivedData/
**/*.xcuserdatad/
**/*.xcuserdata/
**/*.xcscmblueprint
**/*.dSYM.zip
**/*.ipa
**/*.mobileprovision
**/*.provisionprofile

# Firebase(Google) service keys (절대 커밋 금지)
ios/Runner/GoogleService-Info.plist

# =========================
# Node / JS (모노레포 대비 재귀)
# =========================
node_modules/
**/node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 빌드 산출물(웹/백엔드 가능성)
dist/
.out/
.next/
.tmp/
.temp/
.cache/

# =========================
# DB / Runtime / PIDs / Sockets
# =========================
mysql_data/
*.sock
*.pid

# =========================
# Secrets / Environment (절대 커밋 금지)
# =========================
# .env 류: 모든 깊이에서 제외
.env
.env.*
**/.env
**/.env.*
*.env
*.env.*

# 예시 템플릿은 허용
!.env.example
!**/.env.example
!*.example

# 일반 비밀/자격증명
*.secret
*_secret.*
*secret*.json
*credentials*.json
serviceAccount*.json
sentry.properties
my.cnf
*.cnf

# 인증서/키
*.pem
*.crt
*.cer
*.der
*.p7s
*.p12
*.pfx
*.key
id_rsa*
id_dsa*
.ssh/
private.key

# --- Databases / dumps ---
*.sqlite
*.sqlite3
*.db
*.db-journal
*.sql
*.sql.gz
dump*.sql*

# --- OS artifacts (Windows) ---
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Node/Nest/JS 테스트/커버리지 ---
.nyc_output/
coverage/
